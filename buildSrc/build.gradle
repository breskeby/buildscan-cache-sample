apply plugin:"groovy"
apply plugin:"idea"

repositories {
    maven {
      url "https://plugins.gradle.org/m2/"
    }
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile "com.gradle:build-scan-plugin:1.1.1"
    compile 'com.google.guava:guava-jdk5:17.0@jar'
}
